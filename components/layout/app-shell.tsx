'use client';
import Link from 'next/link';import {Home,MessageCircle,PenLine,UserRound,Volume2} from 'lucide-react';import {usePathname} from 'next/navigation';import type {ReactNode} from 'react';
const nav=[['/home',Home,'홈'],['/write',PenLine,'쓰기'],['/dm',MessageCircle,'대화'],['/profile',UserRound,'나'] ] as const;
export function AppShell({children,plain=false}:{children:ReactNode;plain?:boolean}){const path=usePathname();return <main className="min-h-screen bg-cream"><div className="mx-auto min-h-screen max-w-md bg-cream pb-24 md:max-w-3xl"><header className="flex items-center justify-between px-5 py-5"><Link href="/home" className="focus-ring flex items-center gap-2 rounded-lg font-black tracking-tight"><span className="grid size-9 place-items-center rounded-full bg-leaf text-white"><Volume2 size={18}/></span>메아리</Link><span className="text-xs text-black/45">한 사람에게 닿는 마음</span></header>{children}{!plain&&<nav aria-label="주요 메뉴" className="fixed bottom-0 left-1/2 z-30 flex w-full max-w-md -translate-x-1/2 justify-around border-t border-black/5 bg-white/95 px-3 pb-[max(10px,env(safe-area-inset-bottom))] pt-2 backdrop-blur md:max-w-3xl">{nav.map(([href,Icon,label])=><Link key={href} href={href} className={`focus-ring flex min-w-16 flex-col items-center gap-1 rounded-xl p-2 text-xs ${path===href?'text-leaf':'text-black/40'}`}><Icon size={20}/>{label}</Link>)}</nav>}</div></main>}
